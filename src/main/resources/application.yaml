server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nextgenmotor
    username: root
    password: mtmt
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

mqtt:
  broker-url: tcp://broker.hivemq.com:1883
  topic-data: nextgen/motor
  topic-control: nextgen/motor
 